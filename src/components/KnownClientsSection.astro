---
import KnownClientCard from "../components/KnownClientCard.astro";
import SectionHeader from "../components/SectionHeader.astro";

const clients = [
  {
    name: "Mark <br/> Puncher",
    role: "Proffesional boxer",
    record: "24-0-1",
  },
  {
    name: "Mark <br/> Puncher",
    role: "Proffesional boxer",
    record: "24-0-1",
  },
  {
    name: "Mark <br/> Puncher",
    role: "Proffesional boxer",
    record: "24-0-1",
  },
];
---

<section class="container mx-auto py-16">
  <SectionHeader text="We work <br/> with" />

  <div class="flex items-center justify-center pt-14 max-lg:flex-col gap-8">
    {
      clients.map((testimonial) => (
        <KnownClientCard
          name={testimonial.name}
          role={testimonial.role}
          record={testimonial.record}
        />
      ))
    }
  </div>
</section>
